# Secrets / live local config
config.ini
tgif-hblink/hblink.cfg
DVSwitch.ini
stfu/DVSwitch.ini
tgif-hblink/MMDVM_Bridge.hblink.ini

# Runtime-local TGIF/HBLink files
tgif-hblink/MMDVM_Bridge.pre-hblink.ini
tgif-hblink/*.bak
tgif-hblink/MMDVM_Bridge.pre-hblink.ini.lastgood
tgif-hblink/MMDVM_Bridge.pre-hblink.ini.bak
tgif-hblink/MMDVM_Bridge.pre-hblink.ini.keepdonotdelete
tgif-hblink/rules.py
tgif-hblink/peer_ids.json
tgif-hblink/subscriber_ids.json

# Python runtime / caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
dmr_utils3/

# TGIF/HBLink local runtime environment
tgif-hblink/venv/

# Optional upstream/dev leftovers
tgif-hblink/.git/
tgif-hblink/HBmonitor/

# Logs / runtime state
logs/
run/
*.log
*.pid
*.state

# Backup / restore clutter
*.bak
*.bak-*
*.before-step*
*.beforeHBLinkInstall
*.orig
*.old
*.tmp
*.swp
*.swo
*~

# Project backup folders / drafts / scratch
backups/
draft/

# Editor / OS clutter
.DS_Store
Thumbs.db
desktop.ini

# Screenshots / local artifacts
screenshot.png

# Keep example/template configs and core source files tracked
!config.ini.example
!tgif-hblink/hblink.cfg.example
!tgif-hblink/rules.py.template
!tgif-hblink/MMDVM_Bridge.hblink.ini.example
tgif-hblink/config/config.json
tgif-hblink/config/*.local.json
tgif-hblink/config/*.before-*
tgif-hblink/config/*.bak

# TGIFD local runtime / build artifacts
tgif/config/tgifd.ini
tgif/build/
tgif/*.log
tgif/*.log.*

# Local user data
data/favorites.txt
